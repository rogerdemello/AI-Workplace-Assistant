fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 breaks with bcrypt 4.1+ (hash_password / internal verify raises)
bcrypt==4.0.1
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0
# EmailStr in the auth/user schemas needs this; pydantic does not pull it in.
email-validator==2.3.0
redis==5.0.1
httpx==0.26.0
# Azure OpenAI client (app/ai_client) and its retry wrapper. Both were imported
# but undeclared, so any clean install — CI included — failed at collection.
# 2.48.0 resolves against the httpx pin above without upgrading it.
openai==2.48.0
tenacity==9.1.4
pytest==7.4.4
pytest-cov==4.1.0
supabase==2.10.0
apscheduler==3.10.4
sentry-sdk[fastapi]==2.18.0
celery[redis]==5.4.0
pypdf==4.3.1
python-docx==1.1.2
rank-bm25==0.2.2