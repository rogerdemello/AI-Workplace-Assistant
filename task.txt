================================================================================
MARK — TASK & ROADMAP (task.txt)
================================================================================
Use this file to prioritize engineering work: sentiment, chat→HR signals, UX,
and “world class” quality. Update dates/owners as you ship.

Legend: [x] done   [~] partly done (detail inline)   [ ] not started

Last reviewed: 2026-07-28 (main)

--------------------------------------------------------------------------------
0. STATE OF PLAY — read this before picking anything up
--------------------------------------------------------------------------------

Shipped on this branch (not yet merged, CI has not run on it):
   - Conversational requests: appointments, expenses, shift changes/WFH and
     document requests, on the shared employee_requests table, with an HR
     review page. Leave and tickets already existed.
   - Proactive check-in delivery, which was a no-op end to end. Three separate
     breaks: jobs wrote reminders as "cancelled" so dispatch skipped them;
     delivery was SSE-only so it never reached anyone without an open chat;
     and the chat restores from local storage, so persistence alone still
     wasn't delivery. Now: active reminders, durable chat history, and a
     watermarked catch-up fetch on open.
   - Confidentiality fixes on manager surfaces (see C/security below).
   - Alembic owns the Postgres schema; create_all is SQLite-only.
   - /metrics endpoint and real, tunable alert thresholds.

Resolved since the last review:
   1. Everything is merged to main and CI is green (PRs #2-#5). CI itself had
      been red since at least May: openai, tenacity and email-validator were
      imported but never declared, so every clean install died at collection.
   2. The uncommitted work in the tree has been reviewed and landed. It was
      mostly finished bug fixes — including one that mattered, StaticPool on a
      file-backed SQLite URL forcing every request onto one connection.
   3. DEPLOY CHANGE, still current: `alembic upgrade head` is required on every
      deploy. create_all no longer builds the Postgres schema; the boot-time
      audit logs an error if the DB is empty or behind.
   4. CI timing settled: the full suite runs in ~10.5 min on CI (not the ~32
      min seen locally — a local run hits a reachable endpoint that 401s slowly,
      where CI fails DNS instantly). Timeout raised to 45 min for headroom.

Found while building risk explainability:
   [ ] db/schema.sql is a stale partial snapshot — 9 of 53 model tables are
       missing from it (audit_logs, automation_rules, employee_scores,
       hr_notifications, message_signals, offboarding_tasks, sentiment_logs,
       ticket_action_logs, whatsapp_links). Bootstrapping from it gives a
       database where chat works and every HR dashboard is empty. Marked
       REFERENCE ONLY with a pointer to alembic; regenerating it properly from
       a live pg_dump is still open.

Still blocking a confident production claim:
   1. Nothing has been verified against a realistic environment. Every "it
      works" here means tests and a local stack, never staging under load.
   2. No data retention policy (see C).
   3. e2e points at an unreachable model endpoint, so CI exercises a permanent
      -outage path rather than normal operation. Stubbing the AI client would
      be faster and more representative.

Stranded by the uncommitted work above, fix when it is resolved:
   - .env.example additions (DB_CREATE_ALL, alert thresholds) are written but
     uncommitted, because that file also carries someone else's pending edits.
     The settings themselves ARE committed, with working defaults in config.py.
   - new-frontend/e2e/ is entirely untracked, including playwright.config.ts
     and the proactive.spec.ts added here.

--------------------------------------------------------------------------------
1. WHAT NEEDS TO BE DONE
--------------------------------------------------------------------------------

A. Sentiment & signals (core product)
   [x] Every employee chat path persists the sentiment pipeline. Both the
       synchronous and the deferred path are now instrumented — the deferred
       one is the production default (CHAT_DEFER_NONBLOCKING_SIDE_EFFECTS), so
       instrumenting only the sync path would have left failures invisible.
   [ ] Verify in staging under real traffic. Not done — needs an environment.
   [ ] Backfill/reconcile recent messages into sentiment_logs where missing
       (optional; only if prod had partial coverage).
   [ ] HR dashboard smoke test after real chat traffic: confirm sentiment_score,
       trend, risk and top_emotion move. Covered by integration tests, not by
       an observed staging run.

A2. Composer dropping sends — FIXED
   [x] Sends issued while a reply was in flight were swallowed: the input had
       already cleared, no request went out, nothing told the employee. Now
       queued and dispatched in order; the button is no longer disabled on
       transient state; the input clears only on acceptance.
       Confirmed by measurement, not assertion: the appointment spec needed a
       retry that fired on most turns (~56s -> ~2.9m). With the queue the retry
       never fires and the spec is back to 57s, so the retry was removed.
       Covered by new-frontend/e2e/composer.spec.ts.

B. HR visibility & trust
   [x] Screens -> APIs documented: backend/docs/SCREEN_API_MAP.md (all 27
       endpoints verified against app/api/v1, plus the debugging traps: chat
       uses the streaming endpoint, transcripts restore from local storage,
       conversation_id must be threaded, the two SSE streams are role-specific).
   [~] Signal freshness in the UI. The API exposes last_chat_sentiment_at and
       the dashboard bundle carries it; surfacing it consistently in the UI is
       still open. Privacy-safe: no raw text.

C. Quality & operations
   [~] Load test chat + analytics under expected concurrent users.
       Harness exists: backend/scripts/loadtest.py, reports p50/p95/p99 and
       errors per endpoint. NOT YET RUN against a realistic environment — a
       local SQLite run measures SQLite, not the deployment.
   [x] Monitoring: /metrics (HR/admin only) for pipeline throughput, failures
       by error type and path, and HTTP errors by route template. Pipeline
       failures also log event=sentiment_pipeline_failed for log alerting.
       Caveat: per-process, resets on restart — a sample, not a cluster total.
   [x] Security review — RBAC and PII complete.
       Manager visibility: anonymous tickets were counted against named
       reports (on a small team that identifies the author to the manager the
       complaint may be about); appointments and documents raised to HR were
       visible to the employee's manager. Managers now see only shift changes
       and expenses.
       Route guards: every api/v1 route audited. POST /tickets/sla-scan/trigger
       had no auth at all — an unbounded DB job that also reported SLA breach
       counts. The three rooms GETs were public, leaking office layout and
       meeting patterns. A test now holds unauthenticated routes to an explicit
       allowlist with a reason each.
       PII: no message content is logged anywhere, and the audit middleware
       stores only a SHA-256 of the body. Masking gained international and
       Indian mobile formats (the most likely real number in this deployment),
       and the credit-card pattern no longer bites chunks out of UUIDs.
   [~] Data retention — position documented, NOT enforced.
       backend/docs/DATA_RETENTION.md enumerates every table holding employee
       free text, proposes periods, and states plainly that nothing is deleted
       today. Open gaps recorded there: no deletion job, no subject-access or
       erasure path, anonymous tickets still store user_id (anonymity is
       presentation-layer only), no residency position.
       Remaining work is a business decision on the periods, then a job.

D. Product polish (when core is stable)
   [~] Manager vs HR views: scoped data only.
       Fixed for requests and anonymous tickets. OPEN DECISION: managers still
       see per-report sentiment and attrition risk via /manager/team, derived
       from private conversations with Mark. This reads as intended rather than
       accidental, but it deserves a conscious yes/no rather than inheriting it.
   [x] Alerts: configurable thresholds. SENTIMENT_ALERT_THRESHOLD,
       RISK_ALERT_THRESHOLD, ALERT_COOLDOWN_MINUTES, EMOTION_ALERT_TRIGGERS.
       These previously read via getattr() against names never declared on
       Settings, so no deployment could actually tune them.

E. Testing
   [x] Backend: 367 tests.
   [x] Browser end-to-end (Playwright, 12 specs) including the proactive
       check-in round-trip — employee away, HR acts, employee returns and the
       message is waiting; reload does not replay it.
   [x] Playwright runs in CI (browser-e2e job, starts both servers). It has
       already paid for itself: it caught the nudge LIMIT ordering bug, the
       watermark losing HR messages, the flow-opening phrase being captured as
       its own answer, the unreachable-model crash, and the composer send-drop.

--------------------------------------------------------------------------------
2. HOW TO DO IT (practical steps)
--------------------------------------------------------------------------------

Deploying (changed — read this)
   1. `cd backend && alembic upgrade head`. Required now; the app will not
      create Postgres tables for you.
   2. Watch the boot log for schema_audit. An error there means step 1 was
      skipped or a migration is pending.
   3. DB_CREATE_ALL=true forces the old behaviour, for throwaway envs only.

Sentiment pipeline (wired in code — operationalize it)
   1. Deploy backend with latest chat.py pipeline helper.
   2. Run integration tests: pytest backend/tests/test_chat_sentiment_hr.py
   3. Manual: employee sends chat → HR refreshes Pulse/Employees → scores move.
   4. Watch /metrics: sentiment_pipeline_failures_total should stay flat.

HR dashboard verification
   1. Log in as HR → GET /api/v1/analytics/dashboard (or use UI).
   2. Cross-check one employee: SentimentLog rows vs dashboard sentiment_score.
   3. Document any delay (caching, refresh interval).

Load testing
   python -m scripts.loadtest --base-url https://staging.example.com \
       --employees 25 --hr 5 --duration 60
   Run against staging. Compare p95 against whatever you decide is acceptable
   for the chat hot path; there is no agreed budget yet — set one.

Browser e2e (needs both servers up; see playwright.config.ts header)
   cd new-frontend && npm run test:e2e

Safe rollout
   1. Feature flag optional: “strict_pipeline” logging only before full enforce.
   2. Staging → canary → full prod; watch error rate on SentimentPipelineService.

--------------------------------------------------------------------------------
3. HOW TO MAKE IT WORLD CLASS
--------------------------------------------------------------------------------

Accuracy & fairness
   - Move beyond lexicon-only: add a managed model (Azure/OpenAI/other) with
     fallback to rules; evaluate on labeled in-domain data. (Hybrid LLM +
     lexicon fallback exists — SENTIMENT_HYBRID_ENABLED — but has not been
     evaluated against labeled in-domain data.)
   - Multilingual support; test for bias across regions and roles. Not started.

Reliability
   - Idempotent pipeline writes; dead-letter queue for failed processing;
     replay jobs. Not started — failures are now counted and logged, but a
     failed message is still lost rather than retried.

Privacy & compliance
   - Minimize stored raw text; prefer aggregates; clear retention; DPIA-ready
     documentation; role-based redaction in HR UI. Not started. Note that
     proactive nudges now persist to chat history, which adds to the retention
     surface — worth including in that policy.

Explainability
   - HR sees “why” (factors: recent negative ratio, topics, emotion mix) not
     only a single number.

Human in the loop
   - Escalation workflows (HR review, manager coaching) tied to signals, not
     auto-disciplinary use of scores.

Performance
   - Sub-second chat; async pipeline if needed; precomputed dashboard tiles.
     No agreed latency budget exists yet — set one before the load test, or
     the numbers have nothing to fail against.

--------------------------------------------------------------------------------
4. ADVANCEMENT PLANS (phased)
--------------------------------------------------------------------------------

Phase 1 — Solidify (0–4 weeks)
   - [x] End-to-end tests: chat → DB → HR API. Backend + browser both cover it.
   - [x] Staging sign-off checklist: backend/docs/STAGING_SIGNOFF.md, with a
         pass condition per item and a note that the load test needs an agreed
         p95 target beforehand or it cannot fail.
   - [~] Observability baseline: /metrics exists; no dashboards or alerting
         built on top of it yet.
   - [x] HR admin doc: backend/docs/HR_METRICS.md — defines sentiment score,
         trend, risk and top emotion from the code that computes them, and is
         explicit about where they mislead (50 = no history not neutral; the
         10-point guardrail lags reality downward; silence accrues 20% of risk;
         averages are unweighted so 3 messages and 300 look alike).

Phase 2 — Smarter signals (1–3 months)
   - Hybrid sentiment: API + lexicon fallback; calibration to 0–100 scale.
   - Conversation-level scoring (rolling window of last N user messages).
   - Emotion + topic trends: already partially built — expand coverage and UI.

Phase 3 — Proactive & integrated (3–6 months)
   - Rule engine + notifications (email/Slack/Teams) with suppression/cooldowns.
   - Optional HR case/ticket creation from high-risk patterns.
   - Integrations: HRIS for org chart accuracy (manager_id, department).

Phase 4 — Scale & enterprise (6–12 months)
   - Multi-tenant, regional data residency, SSO, audit trails.
   - Formal model governance: versioning, A/B evaluation, fairness reviews.

================================================================================
END
================================================================================
